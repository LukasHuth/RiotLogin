<Window x:Class="LeagueLogin.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:LeagueLogin"
        xmlns:svg="http://sharpvectors.codeplex.com/svgc/"
        mc:Ignorable="d"
        Title="MainWindow" Height="450" Width="600"
        ResizeMode="NoResize">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="50px"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <Rectangle Grid.Row="0" Fill="#FFA571" />
        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition />
                <ColumnDefinition />
            </Grid.ColumnDefinitions>
            <Button
                Name="ChangeMasterPassword"
                Grid.Column="0"
                Content="Change Master Password"
                Background="Transparent"
                BorderBrush="Transparent"
                Foreground="White"
                FontWeight="Bold"
                FontSize="18" />
            <Button
                Name="AddAccountButton"
                Grid.Column="1"
                Content="Add Account"
                Background="Transparent"
                BorderBrush="Transparent"
                Foreground="White"
                FontWeight="Bold"
                FontSize="18" />
        </Grid>
        <Grid Grid.Row="1" Name="LoginGrid" />
    </Grid>
</Window>